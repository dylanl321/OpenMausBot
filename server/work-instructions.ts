export const SHARED_WORK_INSTRUCTIONS = [
  "For new shared deliverables and ongoing multi-bot work, this task policy takes precedence over legacy delegation guidance: first use get_work_item to find existing work, then ensure_work_item to create/reuse its topic and shared task. Include a stable source/account/item identity (not a title or polling timestamp), objective and acceptance criteria.",
  "Organize by outcome, not by tool, bot, message or workflow step. A topic groups related work; a task owns one independently verifiable outcome. Keep implementation, review, decisions and corrections for that outcome in its shared hub and linked specialist threads. Split work only when it has independently finishable outcomes, materially different context/access, or a separate lifecycle. Do not split every discussion or specialist into a new task, or combine unrelated outcomes into one catch-all chat. Reuse existing context and artifact references rather than copying whole conversations. This applies to product delivery, research, incidents, customer requests and other work: an issue key is one useful identity, not a required organizing principle. For work without an external key, choose and reuse a durable project/outcome identity.",
  "When the user says to begin the next work, determine the next actionable priorities from current authorized sources, dependencies, existing tasks and available capacity. Read the backlog directly or use one bounded consultation with the relevant planning teammate if needed, then act on the result in the same request. Choose a small batch within the user's scope and create a separate delivery task for each independent outcome, with concrete acceptance criteria and a short priority rationale in its objective. Group related tasks under their project/topic. Do not substitute a readiness report, audit or planning-only deliverable for an instruction to start execution. Do not ask the user to manually create the focused chats. Explain missing priority/access information instead of inventing rankings; Jira or another tracker remains the external record alongside the collaboration hub, not instead of it.",
  "After creating the selected tasks, end this source turn: their shared hubs now own coordination and return outcomes here automatically. Do not also dispatch a legacy delegation or another coordinator. Unchanged completed or blocked work stays settled; do not invent a new identity or input to restart it. Status-only questions do not authorize starting or reopening work; an explicitly created ongoing goal may continue work within its recorded scope. Only quick standalone advice or bounded priority selection uses the lightweight consultation flow. In a task hub use coordinate_bots with intent=work; linked workers inherit the task. Finish with update_work_item and cite recorded link or event ids as evidence. Use link_item only for work done outside your tools.",
].join("\n");
